今、「カルテを書き出す」というコマンドをいれてくれましたが、これではなく、私が欲しいのはスプレッドシート上の薬の投与履歴をカルテビューに載せることです。
- visithistory sheetの、E列にあるような内容です。
- カルテビューに、同日の投薬情報があれば、その内容を入れたい。
- 単純にHTMLやPNGで書き出す機能がほしい
- 軟膏処方量計算.html で作ったデータは、spredsheetへ保存した場合、スケジュールを作って、患者に共有することができなくなっているのです。

例）
- 顔に、リンデロンを1日2回、2週間（6月10日まで）
- 体前面に、リン/ヘパを1日1回

数回分（たとえば、過去5回分の通算の治療歴）のデータ書き出し、も将来的に考えていますが、後日にします。

---

まだ、反映していないようなので、確認お願いします。今の診療後のフローは、
1. 外来診療前に、QRコードアンケートを行い、POEMなどを患者に入力してもらう。
2. 医師が診療。ダッシュボードでPOEMなどを見つつ、次回再診日を入れる。
3. ダッシュボードで、保存を行い、確認済みに移動させる。
4. atopic_calculator.htmlへのリンクを押す。
   1. 次回再診が入っていることを確認後、薬、塗る量を調節。
   2. 間に合えば、スケジュールをpngでairdropで患者に共有。
   3. atopic_calculatorのデータを保存し、spredsheetに保存。
5. 診療が終わって復讐する。
   1. 診療中に絵を書いて湿疹部位を確認していたものがある。
   2. ダッシュボードの確認済みにある診療録に、EASI scoreなどを入力する。
   3. 薬の投与量、投与部位、などを確認しようとする。

問題は、最後の確認するときに、spredsheetのjsonファイルが見にくいので、確認したいという点。

現在のカルテビューに、「処方内容」が入ったのは、すごく良い。ただ、次のことは、できるでしょうか。

### 目的
診療後にカルテビューを見ているとき。診療中に患者に渡したスケジュールのpngファイルを確認したい。薬の量の計算までは不要。

### 希望のフロー

1. カルテビューを見返したときに、「薬の投与方法を確認する」のようなボタンを置く。
2. ボタンを押すと、その日にatopic_calculator.htmlで作られたjsonファイルが、spredsheetに反映しているか確認する。
3. その結果、データがあれば、「診療中にatopic_calculator.htmlで作成したスケジュール.pngのような絵として」pngやhtmlで書き出す。
4. ここでは、薬の量や1回塗布量までは不要。

カルテビューに処方内容としてテキストが入るのは良いのですが、途中で薬の量を変更したり塗布する薬の種類が多いときは、文字だけだと視認性にかけます。なので、診療中に患者に共有したような画像を作成し、pngなどで出力したデータをクリニックで使っている電子カルテにも保存しておきたいのです。

---

よくなりました。ありがとうございます。次のお願いがあります。

カルテビューを見ると、選んだ人のカルテは見ることができますが、当日診療した他の患者さんをカルテビューで見たい時には、一回ダッシュボードに戻って、確認済みの中から、「確認したい患者」を探し、そのカルテビューを押す必要があります。

そこで、カルテビューの画面で、患者を選択する機能が欲しいです。
例えばカルテビューのUIを変更し、
- 「これまでに診療した患者リスト」などを設置する。
- 「当日受診した患者一覧」のような機能がある。
- 「患者の検索」ができる
などができるようにしてほしいです。

### EASI score や IGA score の経過について。

5月20日のデータが一番左端にきていて、合計3回受診しているような *スコア推移* のグラフになっていますが、この患者さんは **5月20日に受診をされていない** です。どうして5月20日にPOEMが計算されて いる、となってしまうのでしょうか。

---

### IGA scoreの挙動について
ダッシュボードを開いていて、*確認済み* に入っているデータを見ていますが、EASIスコアはクリックした数字が反映されているのですが、IGAスコアは、数字が反映されていません。

ここで、例えば数字の2をクリックして保存し直しても、やっぱり反映されません。

スプレートシートには反映されているのでしょうか。

---

### 画像の保存と連携が取れるのか

現在、EASI Score は iPadとapple pencilを使って絵を描いて面積を求め、点数を出しています。このhtmlファイルは、*gemini* に作ってもらいました。このpngファイルをうまく保存したり、活用できたりできるでしょうか。

もしくは、htmlファイルを使いやすいように修正するのもありです。

画像だと、点数だけだとわからない *湿疹の部位* を残せるので便利かと思っているのです。

---

### データ修正について

過去の記録を修正することが、今だとできます。例えば
- EASIスコア
- IGAスコア
- iPad画像をHTMLでリンク
などです。

ただし、これが、*スプレッドシート上のデータが複製されているのではないか* と疑う時もあります。

現在の設定はどうなっていますか。

例えば、ダッシュボードで *確認済み* に移行している診療録に対して、iPadの画像をリンクさせたいとき。次のどちらですか。
1. その記録済みのデータに *追加されて* 反映される
2. 新しく診療録が複製されて作られる。その作られたデータにiPad画像がリンクされている。

### 質問
1. 過去のデータ（すなわち、*確認済み* の診療録）を修正・加筆・画像添付したいときは、どのようにするのが一番賢いのか。
2. 一番心配される事態は、*本当に必要なデータを消してしまわないのか* 
という点です。

### 私の意見

やはりデータを修正するときには、*アラート表示が出る* のがいいと思います。すなわち、*データを削除していいですか* とかです。

もしくは、確認済みに移行したデータは、やはり *修正できない設定にする* のが安全かもしれないです。

可能ならば、基本的にはデータを追加していく。複製でも良い。ただし、たとえば3日ごとに、同じ日時で同じIDの重なっているデータがスプレッドシート上で見られた場合には、*統合する* といった設定を作る、というのはどうでしょうか。

---

修正ごとに新しい行をどんどん追加していく、というのはありかもしれません。追加された項目（例えばiPad画像が追加されたとき）は、既存の行に入っていた内容（例えば            
      、軟膏処方量の計算）は引き継ぐ。同じ項目に違う内容が入っていたときはどうなりま             
      すか。たとえば、初回の軟膏処方量にはロコイド軟膏が記録されている。でも、2回目に
      iPad画像を保存したときにはリンデロン軟膏のデータも入っていた。カルテビューには 
      ロコイド軟膏とリンデロン軟膏のデータが共存するというのはおかしくなってしまう（             
      おそらく、初回データか2回目の追加データのどちらかがおかしい）。でも、この「spre 
      dsheetには上書きを続けていく」という考えだと、同じ項目（例えばiPad画像）に、異             
      なるデータが共存するとややこしくなりますね。     

---

### 処方量計算について

ロコイド軟膏を試験的に投与するとき、つまり再診を入れないとき。

*この状態だとGoogleスプレッドシートにデータは保存できないのでしょうか?*

再診日が必要ですか?

ときどき、こういうケースもあります。
- 状態が安定している患者さんで、悪化時もしくは薬がなくなったら最新といったケース
- そもそもアトピー性皮膚炎がどうかわからない患者さんで試験的にステロイド軟膏を塗ってくるようにとするケース

---

### *所見メモ* について

カルテダッシュボードの `所見メモ` という欄は、前回記入された文章が継続して残っていくのでしょうか。

スプレッドシートの `clinicalassements` のMの列、`notes` の中。5月25日に受診した時のメモがが、そのまま本日の `所見メモ` に残っている気がします。

---

### カルテビューにおけるIGAスコアとEASIスコアを手入力したときについて

症状が安定している人（EASI 0の人）では、iPadでEASIスコアとIGAスコアを入れません。

この時、カルテビューではEASIスコアもIGAスコアも出てこないですよね。

このとき、ダッシュボードで、手入力で、EASIスコア（0点）と、IGAスコアを入れていいですか。

ただし、09709の患者さんで、EASIスコア 0点（なにも操作せず）、IGAスコア 1点、と入力しましたが、カルテビューには反映されませんでした。